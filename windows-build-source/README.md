# Carpeta histórica

Este directorio conserva una copia antigua del monitor y **no** debe usarse
para generar las versiones actuales con USB Bridge, I²C o SPI.

Para Windows usa el código de la raíz del repositorio y ejecuta:

```powershell
.\scripts\prepare_windows.ps1
```

La guía vigente está en `docs/WINDOWS.md`.
